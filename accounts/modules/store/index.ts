import { databaseORM } from "@/accounts/configuration";
import { TOPICStoreEvents } from "@/packages/types/user";

export async function createStoreAccount(event: TOPICStoreEvents) {
  try {

  } catch (error) {
    console.error(error);
  }
}
